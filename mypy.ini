[mypy]
# sklearn ships no py.typed marker / type stubs; used here strictly for
# comparison and evaluation, never as an implementation dependency, so
# we don't require full stub coverage for it.
[mypy-sklearn.*]
ignore_missing_imports = True
